{
  "name": "opium-nav",
  "version": "1.0.0-alpha1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "start": "react-scripts start",
    "watch": "npm run clear && nodemon -e ts,tsx -w ./src -x npm run build",
    "link": "npm link react && npm link",
    "unlink": "npm unlink ract",
    "build": "tsc --project tsconfig.dist.json",
    "clear": "rm -fr ./dist",
    "dist": "npm run build && rsync -a ./dist/ ../3p5-admin/node_modules/3p5-hooks/dist/",
    "publish": "npm run build && npm publish"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^16.0.0",
    "@types/react": "^17.0.13",
    "nodemon": "^2.0.12",
    "react": "^17.0.1",
    "react-scripts": "^4.0.3",
    "typescript": "^4.3.5"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
